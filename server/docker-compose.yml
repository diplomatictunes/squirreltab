version: '3.8'

services:
  server:
    build: .
    ports:
      - "6745:8000"
    volumes:
      - .:/app
      - db_data:/app/data
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always

volumes:
  db_data:
